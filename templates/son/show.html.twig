{% extends 'base.html.twig' %}

{% block title %}Son{% endblock %}

{% block body %}
    <h1>Son</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ son.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ son.nom }}</td>
            </tr>
            <tr>
                <th>Son</th>
                <td>{{ son.son }}</td>
            </tr>
            <tr>
                <th>AuteurNom</th>
                <td>{{ son.auteurNom }}</td>
            </tr>
            <tr>
                <th>AuteurPrenom</th>
                <td>{{ son.auteurPrenom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ son.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('son_index') }}">back to list</a>

    <a href="{{ path('son_edit', {'id': son.id}) }}">edit</a>

    {{ include('son/_delete_form.html.twig') }}
{% endblock %}
