<form method="post" action="{{ path('code_barre_delete', {'id': code_barre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code_barre.id) }}">
    <button class="btn">Delete</button>
</form>
