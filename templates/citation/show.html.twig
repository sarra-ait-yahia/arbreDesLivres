{% extends 'base.html.twig' %}

{% block title %}Citation{% endblock %}

{% block body %}
    <h1>Citation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ citation.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ citation.auteur }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ citation.text }}</td>
            </tr>
            <tr>
                <th>RapporteurNom</th>
                <td>{{ citation.rapporteurNom }}</td>
            </tr>
            <tr>
                <th>RapporteurPrenom</th>
                <td>{{ citation.rapporteurPrenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('citation_index') }}">back to list</a>

    <a href="{{ path('citation_edit', {'id': citation.id}) }}">edit</a>

    {{ include('citation/_delete_form.html.twig') }}
{% endblock %}
